apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kyverno
  namespace: kyverno
spec:
  interval: 1h0m0s
  chart:
    spec:
      chart: kyverno
      version: "3.x.x"
      sourceRef:
        kind: HelmRepository
        name: kyverno
      interval: 1h0m0s
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace